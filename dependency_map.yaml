services:
  OmniNoteService:
    models:
      - OmniNote
      - Attachment
      - Task
      - Goal
      - Event
  DayReflectionService:
    models:
      - DayReflection
  TrackerService:
    models:
      - Tracker
      - TrackerType
  TaskService:
    models:
      - Task
  NotificationService:
    models:
      - Tracker
  UserProfileService:
    models:
      - UserProfile
      - Settings
  SyncService:
    models:
      - OmniNote
      - SyncMetadata
  AIService:
    models:
      - OmniNote
  TimezoneHelperService:
    models: []  # purely helper, no direct model deps

pages:
  JournalPage:
    services:
      - OmniNoteService
    models:
      - OmniNote
  NoteDetailPage:
    services:
      - OmniNoteService
      - TrackerService
    models:
      - OmniNote
      - Tracker
      - TrackerType
  CalendarOverviewPage:
    services:
      - OmniNoteService
    models:
      - OmniNote
  DayReflectionPage:
    services:
      - DayReflectionService
    models:
      - DayReflection
  TrackerPage:
    services:
      - TrackerService
    models:
      - Tracker
      - TrackerType
  TrackerDetailPage:
    services:
      - TrackerService
      - OmniNoteService
    models:
      - Tracker
      - OmniNote
  MediaPage:
    services: []    # currently no direct service calls
    models: []
  GardenForestPage:
    services:
      - OmniNoteService
    models:
      - OmniNote
      - ZoneTheme
  StatsPage:
    services:
      - OmniNoteService
    models:
      - OmniNote
      - ZoneTheme
  TimeGroupJournalPage:
    services:
      - OmniNoteService
    models:
      - OmniNote
      - ZoneTheme
