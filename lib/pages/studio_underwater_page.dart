import 'package:flutter/material.dart';

class StudioUnderwaterPage extends StatelessWidget {
  const StudioUnderwaterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Studio / Underwater')),
      body: const Center(child: Text('Studio / Underwater Page')),
    );
  }
}
