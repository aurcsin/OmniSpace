import 'package:flutter/material.dart';

class SkySpacePage extends StatelessWidget {
  const SkySpacePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sky / Space')),
      body: const Center(child: Text('Sky / Space Page')),
    );
  }
}
